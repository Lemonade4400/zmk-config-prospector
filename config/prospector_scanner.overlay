/*
 * Prospector Scanner - User Overlay
 *
 * No SPI frequency override - using module default (20 MHz).
 * If using long dupont wires, reduce to 4 MHz:
 *   &st7789v { mipi-max-frequency = <4000000>; };
 */
